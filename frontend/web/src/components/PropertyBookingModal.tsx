"use client";
import { useState } from "react";
import { CalendarDays, Mail, Phone, X } from "lucide-react";
interface Props { propertyTitle:string; agentName:string; agentPhone:string; agentEmail:string; }
export default function PropertyBookingModal({propertyTitle,agentName,agentPhone,agentEmail}:Props){const [open,setOpen]=useState(false); const [time,setTime]=useState(""); return <><button type="button" onClick={()=>setOpen(true)} className="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-slate-900 py-3 text-sm font-semibold text-white hover:bg-slate-800"><CalendarDays className="h-4 w-4"/>Đặt lịch xem nhà</button>{open&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/55 p-4" role="dialog" aria-modal="true" aria-labelledby="booking-title"><div className="relative w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><button type="button" onClick={()=>setOpen(false)} aria-label="Đóng" className="absolute right-4 top-4 rounded-full p-2 text-slate-500 hover:bg-slate-100"><X className="h-5 w-5"/></button><h2 id="booking-title" className="pr-8 text-xl font-bold text-slate-900">Đặt lịch xem nhà</h2><p className="mt-2 text-sm text-slate-600">{propertyTitle}</p><label className="mt-5 block text-sm font-semibold text-slate-700">Thời gian mong muốn<input type="datetime-local" value={time} onChange={(event)=>setTime(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 p-3 text-sm font-normal"/></label><p className="mt-4 text-sm text-slate-600">Liên hệ {agentName} để xác nhận lịch xem{time?` vào ${new Date(time).toLocaleString("vi-VN")}`:""}.</p><div className="mt-5 grid gap-2 sm:grid-cols-2"><a href={`tel:${agentPhone.replace(/\s+/g,"")}`} className="inline-flex items-center justify-center gap-2 rounded-xl bg-blue-600 py-3 text-sm font-semibold text-white"><Phone className="h-4 w-4"/>Gọi tư vấn</a><a href={`mailto:${agentEmail}?subject=${encodeURIComponent(`Đặt lịch xem ${propertyTitle}`)}&body=${encodeURIComponent(time?`Thời gian mong muốn: ${new Date(time).toLocaleString("vi-VN")}`:"")}`} className="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-200 py-3 text-sm font-semibold text-slate-700"><Mail className="h-4 w-4"/>Gửi email</a></div></div></div>}</>}
